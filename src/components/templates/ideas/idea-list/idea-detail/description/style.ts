import { makeStyles } from "@fluentui/react-components";

export const useStyles = makeStyles({
  descriptionTitle: {
    margin: "0",
  },
  descriptionBlockReadMoreButton: {
    width: "fit-content",
    border: "none",
    background: "transparent",
    color: "#384EC1",
    cursor: "pointer",
  },
});
