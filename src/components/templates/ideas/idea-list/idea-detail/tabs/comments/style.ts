import { makeStyles } from "@fluentui/react-components";

export const useStyles = makeStyles({
  comments: {},
  editor: {
    height: "174px",
    paddingBottom: "42px",
  },
  submitComment: {
    marginTop: "32px",
    width: "100%",
    textAlign: "right",
  },
});
